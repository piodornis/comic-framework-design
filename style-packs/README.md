# Style Packs

Style Packs define reusable visual languages.

A reusable pack should describe appearance, rendering, color, linework, composition, and consistency anchors without embedding one comic project's narrative canon.

Project-specific styles should normally live in the comic project's own `design/styles/` directory.

Current reusable pack:

- `franco-belgian-vivid`
